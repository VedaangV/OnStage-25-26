navigate to:
/lib/systemd/system/bluetooth.service.d/nv-bluetooth-service.conf

edit line:
ExecStart=/usr/lib/bluetooth/bluetoothd -d --noplugin=audio,a2dp,avrcp
to:
ExecStart=/usr/lib/bluetooth/bluetoothd -d

run:
sudo apt-get update
sudo apt-get install pulseaudio-module-bluetooth

CODE FOR AUDIO MODULES:

import pygame

pygame.mixer.init()
pygame.mixer.set_num_channels(4)
MINING_CHANNEL = pygame.mixer.Channel(0)
MINING_AUDIO = pygame.mixer.Sound("ICE.mp3")

# play audio for ice
def play_sound_ice(id): # id of robot initiating the sound
  audio = pygame.mixer.Sound("ICE.mp3")
  MINING_CHANNEL.stop()
  MINING_CHANNEL.play(audio)


  
